#include "linearlist.hpp"
#include "linkedlist.hpp"

int main()
{
	//linearlist<int32_t> test(10U);
	//for (size_t i = 0U; i < test.MaxSize() - 1U; ++i)
	//{
	//	test.Set(i, (int32_t)(i * 3 - 1));
	//}

	//test.Insert(2, -20);
	//test.Insert(7, -30);

	//test.Set(0, -40);
	//test.Set(8, -50);

	//test.Delete(6);

	//linearlist<int32_t> testMove(std::move(test));
	
	return 0;
}