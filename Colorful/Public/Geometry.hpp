NamespaceBegin(Geometry)

void Model::CreateFromObj(const uint8_t *pData)
{
	assert(pData);
}

void Model::CreateFromTxt(const uint8_t *pData)
{
	assert(pData);
}

void Model::CreateFromSDKMesh(const uint8_t *pData)
{
	assert(pData);
}

NamespaceEnd(Geometry)