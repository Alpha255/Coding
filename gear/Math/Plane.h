#pragma once

#include "Vector4.h"
#include "Functions.h"

namespaceStart(gear)
namespaceStart(math)

class plane : public vec4
{
public:
protected:
private:
};

namespaceEnd(math)
namespaceEnd(gear)
