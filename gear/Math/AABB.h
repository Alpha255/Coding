#pragma once

#include "Vector4.h"
#include "Functions.h"

namespaceStart(gear)
namespaceStart(math)

class aabb
{
public:
protected:
private:
	vec3 m_Min;
	vec3 m_Max;
};

namespaceEnd(math)
namespaceEnd(gear)
