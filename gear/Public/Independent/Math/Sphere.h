#pragma once

#include "Vector4.h"

namespaceStart(gear)
namespaceStart(math)

class sphere
{
public:
protected:
private:
	vec3 m_Center;
	float32_t m_Radius;
};

namespaceEnd(math)
namespaceEnd(gear)
